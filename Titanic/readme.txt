First of all you need to  install this project in IDE
then open the new terminal in the IDE
then, install the flask by typing "pip install flask" in  opened terminal
then run this project by typing the "python app.py"  in opened terminal